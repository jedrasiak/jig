#include <stdio.h>
#include "nodes.h"

/**
 * Standalone executable for debugging the nodes function
 * Build with: make (from src/nodes/ directory)
 */
int main(int argc, char **argv) {
    return nodes(argc, argv);
}

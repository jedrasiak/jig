#include <stdio.h>
#include "edges.h"

/**
 * Standalone executable for debugging the edges function
 * Build with: make (from src/edges/ directory)
 */
int main(int argc, char **argv) {
    return edges(argc, argv);
}

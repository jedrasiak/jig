#include <stdio.h>
#include "filter.h"

/**
 * Standalone executable for debugging the filter() function
 * Build with: make (from this directory)
 */
int main(int argc, char **argv) {
    return filter(argc, argv);
}
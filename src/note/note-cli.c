#include <stdio.h>
#include "note.h"

/**
 * Standalone executable for debugging the note() function
 * Build with: make (from this directory)
 */
int main(int argc, char **argv) {
    return note(argc, argv);
}

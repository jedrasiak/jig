#include <stdio.h>
#include "find.h"

/**
 * Standalone executable for debugging the find() function
 * Build with: make (from this directory)
 */
int main(int argc, char **argv) {
    return find(argc, argv);
}
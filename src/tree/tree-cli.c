#include <stdio.h>
#include "tree.h"

/**
 * Standalone executable for debugging the tree function
 * Build with: make (from this directory)
 */
int main(int argc, char **argv) {
    return tree(argc, argv);
}
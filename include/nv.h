#ifndef NV_H
#define NV_H

/**
 * Creates a new vertex directory with markdown files for each language.
 *
 * @param path The path where the vertex directory should be created
 * @return 0 on success, 1 on failure
 */
int nv(char *path);

#endif // NV_H

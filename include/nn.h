#ifndef NN_H
#define NN_H

/**
 * Creates a new note directory with markdown files for each language.
 *
 * @param path The path where the note directory should be created
 * @return 0 on success, 1 on failure
 */
int nn(char *path);

#endif // NN_H
